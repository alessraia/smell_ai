"""Prompt Engineering (UC02) UI package.

This package is intentionally kept outside `llm_detection` to keep boundaries
clear:
- `llm_detection`: domain model, persistence, providers, orchestration.
- `prompt_engineering`: UI and UC02-specific interaction flow.
"""
